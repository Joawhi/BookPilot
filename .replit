run = "npm run dev"
entrypoint = "package.json"
hidden = [".git", "node_modules", "data"]
modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

[deployment]
build = ["sh", "-c", "npx prisma db push && npm run build"]
run = ["npm", "start"]

[[ports]]
localPort = 3001
externalPort = 80
